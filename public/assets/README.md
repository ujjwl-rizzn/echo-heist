Runtime-generated art and synthesized audio keep this build lightweight and deployment-friendly.

Suggested replacement folders:
- `audio/` for music loops and SFX
- `images/` for promo art or custom sprites
- `fonts/` for packaged display fonts
